const GEMINI_API_KEY = import.meta.env.VITE_GEMINI_API_KEY;\nconst GEMINI_API_URL = 'https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent';\n\nexport const sendToGemini = async (prompt) => {\n  if (!GEMINI_API_KEY) {\n    throw new Error('Gemini API key not configured. Please set VITE_GEMINI_API_KEY in .env.local');\n  }\n\n  try {\n    const response = await fetch(`${GEMINI_API_URL}?key=${GEMINI_API_KEY}`, {\n      method: 'POST',\n      headers: {\n        'Content-Type': 'application/json',\n      },\n      body: JSON.stringify({\n        contents: [\n          {\n            parts: [\n              {\n                text: prompt,\n              },\n            ],\n          },\n        ],\n        generationConfig: {\n          temperature: 0.7,\n          topK: 40,\n          topP: 0.95,\n          maxOutputTokens: 2048,\n        },\n      }),\n    });\n\n    if (!response.ok) {\n      const error = await response.json();\n      throw new Error(error.error?.message || 'Gemini API error');\n    }\n\n    const data = await response.json();\n    \n    if (!data.candidates || data.candidates.length === 0) {\n      throw new Error('No response from Gemini');\n    }\n\n    return data.candidates[0].content.parts[0].text;\n  } catch (error) {\n    console.error('Gemini API Error:', error);\n    throw error;\n  }\n};\n\nexport const generateCode = async (description) => {\n  const prompt = `You are an expert code generator. Generate clean, production-ready code based on this description:\n\n${description}\n\nProvide only the code without explanations. Use modern syntax and best practices.`;\n  \n  return sendToGemini(prompt);\n};\n\nexport const analyzeCode = async (code) => {\n  const prompt = `Analyze this code and provide suggestions for improvement, potential bugs, and best practices:\n\n\\`\\`\\`\n${code}\n\\`\\`\\`\n\nBe concise and practical.`;\n  \n  return sendToGemini(prompt);\n};\n\nexport const explainCode = async (code) => {\n  const prompt = `Explain what this code does in clear, beginner-friendly language:\n\n\\`\\`\\`\n${code}\n\\`\\`\\`\n\nInclude what each part does and why.`;\n  \n  return sendToGemini(prompt);\n};\n"